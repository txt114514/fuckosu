在任务开始前，你你需要梳理代码结构和遵守已经提到的代码规范，这些在各个项目下的md和AGENT.md中

请以 `Plan Gap Audit（检查日期：2026-06-24）` 为唯一任务清单，一次性完成其中所有“真正未完成”的 P0、P1、P2 项，并同步修正“文档需更新”中的全部过期内容。不要只生成计划、代码框架、TODO、伪实现或部分补丁；请直接修改代码、配置、schema、测试和文档，并在完成后推进到尽可能大规模的真实测试。

本任务不接受“先完成一部分，剩余以后处理”。除非受到当前环境中客观不可消除的硬件、数据或权限限制，否则不得提前结束。

# 一、总体目标

完成以下闭环：

```text
显式坐标标定
→ 正确动作标签
→ 空间训练
→ 候选缓存
→ 时序训练
→ 决策导出
→ 固定评估集评分
→ 错误归因
→ 下一轮 trial/job 生成
→ runner/CLI 消费
→ gallery、score、artifact 和版本信息统一保存
→ artifact 反加载推理验证
→ 大规模测试
```

最终结果必须达到：

1. 所有 P0、P1、P2 项都有真实实现；
2. 所有实现都接入当前实际训练链路，而不是孤立工具函数；
3. 所有新增配置都能从当前 Settings、CLI 和训练流程传递到实际执行位置；
4. 所有版本字段都参与保存、加载和兼容性校验；
5. 自动优化闭环至少能完成“本轮评分 → 归因 → 下一轮 job JSON → runner/CLI 消费执行”；
6. 默认不启动无限训练循环；
7. 不再以 1 帧、1 step、1 个样本作为最终验证；
8. 完成单元测试、集成测试、artifact smoke test 和尽可能大规模的真实训练测试。

# 二、开始前必须检查

开始修改前，先检查但不要停下来等待确认：

* 项目当前 Python 版本和最低支持版本；
* PyTorch、CUDA、Typer、Pydantic、pytest、Ruff 版本；
* 当前 GPU、显存、CPU、内存和磁盘空间；
* 当前数据集规模；
* 当前配置文件及训练入口；
* `src/traning/docs` 中六个模块 plan；
* `TRAINING_PLAN.md`；
* `TRAINING_READINESS.md`；
* `OPTIMIZATION_MODULE.md`；
* 当前测试目录和已有测试约定。

不要预先假设 Python 是 3.8。代码必须兼容项目实际声明的最低 Python 版本。

如果多个配置文件中的 Python 版本不一致，不要擅自升级环境；以当前实际运行环境和正式项目配置为依据，并在最终报告中明确说明。

# 三、工程结构限制

严格遵循最小必要修改原则。

不得：

1. 重建项目目录结构；
2. 移动或重命名现有文件；
3. 大规模拆分或合并模块；
4. 新建与现有体系重复的 service、controller、handler、application 层；
5. 替换现有 Settings、训练流水线、Typer、pytest 或配置系统；
6. 修改模型任务定义或训练目标语义；
7. 修改与本次缺口无关的代码；
8. 对整个项目执行无关格式化；
9. 为通过测试而删除现有能力；
10. 使用占位实现、空函数、固定返回值或跳过断言。

允许：

* 在现有模块中增加必要的数据模型、配置字段和辅助函数；
* 按现有目录风格增加确有必要的测试文件、schema migration 文件或导出索引文件；
* 对直接涉及缺口的函数做局部重构；
* 为消除重复默认值增加模块级常量；
* 更新与代码真实状态不一致的文档。

CLI 和业务逻辑必须继续保持分离：

```python
def run_training(training_config: Path) -> None:
    ...
```

```python
@app.command("train")
def train(
    training_config: Path = typer.Option(...),
) -> None:
    run_training(training_config)
```

Typer 只能负责命令行解析和调用普通 Python 业务函数。不得把 `typer.Option`、`typer.Argument`、`typer.Exit` 或 `typer.echo` 放入训练、评估、导出、评分和优化业务函数中。

# 四、必须完成的 P0

## 4.1 显式 playfield 坐标标定

消除 `OsuVideoTransform.fit_centered(...)` 作为实际训练链路中的默认隐含假设。

必须实现：

1. 在合适的配置、数据版本或 segment 元数据中加入显式 playfield rect；
2. 明确 rect 使用的视频像素坐标格式，例如：

   * `left`
   * `top`
   * `width`
   * `height`
3. 明确裁剪前后、缩放前后和实际视频帧之间的坐标空间；
4. 提供稳定版本号，例如 transform/schema version；
5. 统一接入：

   * `spatial_targets`
   * `decision/generator`
   * `visualization/render`
   * `optimization/scoring/run_outputs`
   * gallery 标签绘制
   * 候选与预测坐标转换
6. 保证 osu 坐标与实际 playfield rect 内视频像素坐标双向转换；
7. 保留必要的旧数据兼容策略，但不得静默继续使用错误居中假设；
8. 缺少标定信息时应明确报错、警告或使用显式声明的 legacy 模式；
9. 为非居中、非全幅、不同宽高比 playfield 编写双向转换测试；
10. 验证 round-trip 误差和边界点转换。

不得只修 gallery 绘制，必须修正标签生成、训练监督、评分和决策全链路。

## 4.2 完整 circle 动作状态

修复 `beatmap_action_v1`。

普通 circle 必须形成：

```text
no-op → press → release → no-op
```

必须明确并实现：

* press 时间窗口；
* release 时间点或窗口；
* press/release 同帧或相邻帧时的确定性规则；
* 不同 FPS 和时间量化下的行为；
* 与 slider/spinner 动作状态的一致定义；
* 连续 circle 的按键状态衔接。

为以下情况补测试：

* 单 circle；
* 两个间隔较大的 circle；
* 两个极密集 circle；
* release 与下一次 press 相邻；
* 不同视频 FPS；
* hit time 落在帧边界附近。

## 4.3 slider repeat、spinner 与密集重复点

必须使用 slider 的 `repeats` 和持续时间信息，而不是只处理 start/end。

明确并实现：

* slider start；
* slider hold；
* repeat 边界；
* repeat 方向变化；
* slider end/release；
* spinner start/hold/end/release；
* 极密集重复点的最小间隔策略；
* 同帧 press/release 冲突优先级；
* `min_click_interval_ms` 与动作标签、决策和评分之间的关系。

不得把复杂 slider 当作单一 start/end 长按。

测试至少覆盖：

* repeat=1；
* repeat>1；
* 多次 repeat；
* spinner；
* slider 结束后立即 circle；
* 连续重复点；
* 时间边界恰好落在采样帧上。

## 4.4 固定评估集和版本校验

完整接入并强制使用：

* dataset version；
* evaluation dataset version；
* score version；
* candidate cache version；
* transform/playfield version；
* code version；
* configuration version。

`EvaluationRunMetadata.dataset_version` 不得继续只存在于 schema 中。

必须写入并校验：

* 完整训练评分输出；
* `trial_score_report.json`；
* gallery request；
* gallery manifest；
* optimization trial 记录；
* model artifact manifest；
* next trial/job JSON；
* experiment comparison。

当两个 trial 的关键版本不一致时：

* 默认禁止直接比较或晋级；
* 输出明确错误或 incompatible 状态；
* 只有通过显式 override 才允许比较；
* override 必须写入报告，不能静默绕过。

code version 优先使用 Git commit；工作区有未提交修改时记录 dirty 状态。无法获得 Git 信息时使用明确的 fallback，而不是空字符串。

## 4.5 自动优化闭环

将当前已有：

* `plan_next_trial`
* `execute_optimization_plan`
* `TrainingJobSpec`
* score report
* attribution

接入完整训练结束流程。

至少实现以下单轮闭环：

```text
训练完成
→ 固定评估集评分
→ 错误归因
→ 参数调整计划
→ trial 记录
→ 下一轮 TrainingJobSpec JSON
→ runner/CLI 读取并执行
```

要求：

1. 默认只生成并执行受控数量的下一轮 job；
2. 不允许默认无限递归训练；
3. 提供明确的最大 trial 数、最大 stage、预算和停止条件；
4. 保存父 trial、子 trial、checkpoint 继承关系；
5. 保存 ASHA rung、课程阶段和预算；
6. 记录 prune、promote、pass、fail 原因；
7. 支持 dry-run；
8. 支持只生成 job 不执行；
9. 支持 runner/CLI 消费已有 job JSON；
10. runner 必须调用普通 Python 业务函数，而不是复制训练逻辑；
11. 中断后能够根据已有 trial/job 状态恢复，不重复覆盖有效结果；
12. 不改变现有单轮训练命令的默认兼容行为。

# 五、必须完成的 P1

## 5.1 loss 权重配置化

将硬编码：

```text
action + candidate + xy + 0.01 * time
```

改为 Settings 中明确、可验证、可搜索的配置字段。

至少包括：

* action loss weight；
* candidate selection loss weight；
* xy loss weight；
* time offset loss weight。

要求：

* 有合理默认值，保持当前行为兼容；
* 配置经过 schema 校验；
* 写入训练 summary、artifact manifest 和 trial 参数；
* optimization 可以修改这些权重；
* 实际训练 loop 必须读取该配置；
* 测试验证修改权重会改变最终 loss 组合。

## 5.2 `min_click_interval_ms` 全链路接入

将 `Settings.evaluation.min_click_interval_ms` 注入：

* 时序评估；
* decision 错误归因；
* `TrialScoreSpec`；
* 完整训练评分；
* optimization scoring；
* trial report；
* gallery/manifest 中必要的评估元数据。

不得继续依赖 `TrialScoreSpec()` 的静态默认值覆盖用户配置。

## 5.3 因果一致性测试

增加明确测试：

> 修改未来帧、未来候选或未来特征，不得改变已经产生的过去输出。

至少覆盖：

* `CausalTemporalModel.step`；
* reset 前后状态；
* 分段执行与连续执行一致性；
* 修改未来窗口；
* batch/sequence 推理与 step 推理的过去输出一致性；
* 缓存状态不串样本。

测试必须能在未来模型错误引入双向注意力、未来 padding 或未来特征泄漏时失败。

## 5.4 slider 拓扑 data check

扩展 data check，显式报告：

* slider 交叉；
* 自交；
* 接触分叉；
* 多 head 竞争同一连通分量；
* 无法唯一确定 head/path；
* 退化路径；
* 极短 slider；
* 异常 repeat；
* 超出 playfield；
* 缺失或非法几何信息。

报告中至少包含：

* segment；
* beatmap/item 标识；
* 问题类型；
* 严重程度；
* 是否阻塞训练；
* 可定位的时间或对象索引。

## 5.5 更细数据分布统计

扩展 `DataInputReport`，至少增加：

* spinner 数量与持续时间分布；
* long sequence 数量与长度分布；
* slider repeat 分布；
* circle 密集度；
* min inter-object interval 分布；
* 长 slider；
* 高密度窗口；
* 重复点；
* circle/slider/spinner 比例；
* 每个 segment 的有效帧和对象覆盖。

输出应适合课程采样和启动前检查，并保存为机器可读格式。

## 5.6 manifest 完整版本信息

gallery、score、artifact 和 run manifest 至少保存：

* dataset version；
* evaluation dataset version；
* score version；
* candidate cache version；
* transform version；
* configuration version；
* code version；
* trial ID；
* parent trial ID；
* checkpoint ID/hash；
* 创建时间；
  -关键训练参数。

## 5.7 HTML 或 CSV 索引

至少生成一个稳定 CSV 索引；如果现有依赖允许，再生成静态 HTML。

索引应支持按以下字段浏览或过滤：

* 错误类型；
* segment；
* beatmap；
* sample；
* trial；
* curriculum stage；
* 参数组合；
* score；
* gallery 图片路径；
* predicted/target 坐标；
* action 类型；
* ambiguity reason；
* 版本信息。

不得引入大型 Web 框架。

## 5.8 artifact 推理 smoke test

不能只检查复制和 sha256。

测试必须：

1. 从导出 artifact 重新加载配置；
2. 重新构建或加载空间模型；
3. 加载模型权重；
4. 对一帧合法输入运行空间推理；
5. 读取候选或构造合法候选；
6. 加载时序模型；
7. 执行至少一次 temporal step；
8. 检查输出 schema、形状、dtype、有限值和设备兼容；
9. 在 CPU 环境也能执行最小 smoke；
10. 明确验证 artifact 不依赖原训练目录中的隐藏文件。

## 5.9 schema migration

实现旧配置或旧 artifact schema 到当前 `conf` 模型的迁移流程。

要求：

* 每次 migration 有版本号；
* 顺序可重复执行；
* 已是当前版本时不重复修改；
* 保存 migration log；
* 无法迁移时给出明确错误；
* 不静默丢弃未知关键字段；
* 为至少一个旧版 fixture 编写迁移测试；
* 迁移后能够通过当前 Settings/schema 校验；
* artifact 导入流程实际调用 migration。

# 六、必须完成的 P2

P2 虽不阻塞首版，但本次任务要求全部完成，不得留为 TODO。

## 6.1 候选局部 refiner

对少量候选进行 stride-1 或高分辨率局部复查。

要求：

* 有明确触发条件；
* 限制候选数和显存；
* 不对全帧无差别高分辨率重算；
* 输出 refinement 前后坐标和置信度；
* 可通过配置关闭；
* 实际接入候选生成；
* 有单元和集成测试。

## 6.2 条件歧义复查

对以下情况进行二次复查：

* 低置信；
* 前两候选分数接近；
* slider head/path 冲突；
* 多 patch 竞争；
* ring 几何不一致；
* ambiguity reason 已标记。

要求：

* 明确复查策略；
* 保留原始候选和复查结果；
* manifest 中记录复查原因和变化；
* 不允许无上限重复复查；
* 无法消除歧义时明确保留 ambiguous 状态。

## 6.3 空间一致性 loss 接入

将现有 `lib/training/losses.py` 中相关底层函数实际接入空间训练 loop：

* 跨 patch embedding 一致性；
* ring 半径一致性；
* slider continuity loss。

要求：

* 权重可配置；
* 默认值保持现有行为或采用明确的兼容策略；
* summary 中记录各项 loss；
* optimization 可以调整；
* 对无对应目标的 batch 正确跳过；
* 不产生 NaN；
* 有梯度和数值测试。

## 6.4 自动关联 run、gallery、score 和 artifact

完整训练结束后自动形成统一实验包或稳定关联关系。

至少保证：

* run ID 一致；
* trial ID 一致；
* artifact manifest 指向 gallery 和 score；
* gallery manifest 指向模型 artifact 和 score；
* score report 指向候选缓存、decision 输出和模型 checkpoint；
* 路径优先使用 artifact 内相对路径；
* 复制到其他目录后仍可解析；
* hash 校验可识别文件不一致；
* 不依赖人工根据目录名猜测对应关系。

# 七、文档同步更新

更新以下过期内容：

1. `DECISION_PLAN.md`

   * 更新 `TRAINING_STAGES` 的真实注册状态。

2. `TRAINING_READINESS.md`

   * 更新真实评估集评分桥接状态；
   * 更新当前入口；
   * 明确是否保留 `traning.cli` 兼容别名。

3. `OPTIMIZATION_MODULE.md`

   * 加入 `scoring/run_outputs.py`；
   * 更新自动闭环的新入口、job schema 和停止策略。

4. `RESULT_EXPORT_PLAN.md`

   * 更新 `predicted_video_xy`；
   * 更新 manifest、索引和 artifact 关联格式。

5. 所有相关 plan

   * 将本次已完成项标记为已实现；
   * 文档必须对应真实路径、真实函数和真实命令；
   * 不得仅修改“完成”标记而不实现代码。

# 八、测试要求

必须依次执行以下层级。

## 8.1 静态和快速测试

运行：

* 受影响文件的 Ruff；
* 项目已有类型检查（如果已配置）；
* 新增单元测试；
* 受影响模块已有测试；
* 全量单元测试。

不得只运行单个新增测试后宣布完成。

## 8.2 集成测试

至少完整运行：

```text
data check
→ spatial target generation
→ spatial training
→ candidate cache
→ temporal target generation
→ temporal training
→ decision export
→ fixed evaluation scoring
→ attribution
→ next job planning
→ job consumption
→ gallery/index export
→ model artifact export
→ artifact reload inference
```

集成测试必须使用真实文件格式和真实模块连接。可以 mock 外部网络，但不得把核心训练链路全部 mock 掉。

## 8.3 边界和回归测试

至少覆盖：

* 非居中 playfield；
* 不同分辨率；
* 不同 FPS；
* circle release；
* slider repeat；
* spinner；
* 密集重复点；
* 候选歧义；
* 跨 patch slider；
* 空候选；
* 无有效训练样本；
* 旧 schema migration；
* 版本不兼容；
* artifact 缺失或 hash 错误；
* CPU artifact smoke；
* 因果未来泄漏；
* 中断恢复；
* next job dry-run；
* 最大 trial 停止条件。

# 九、大规模测试要求

完成所有快速测试和集成测试后，直接推进到大规模测试，不要再次询问确认。

“大规模”不得定义为 1 step、1 帧或 1 个样本。

先根据当前硬件和数据规模自动选择可执行预算。

至少分为三级：

## Level A：中等规模稳定性测试

使用真实数据的分层样本，覆盖：

* circle；
* slider；
* repeated slider；
* spinner；
* high-density；
* long sequence；
* ambiguous；
* 多分辨率；
* 多 segment。

要求：

* 空间训练至少运行到多个 optimizer step；
* 时序训练至少运行到多个 sequence batch；
* 候选缓存和决策导出覆盖数百帧或当前数据允许的最大合理规模；
* 完整生成 score、gallery、index 和 artifact；
* artifact 重新加载推理。

## Level B：扩大训练测试

在 Level A 通过后：

* 扩大 segment 数；
* 扩大帧数；
* 扩大训练 step；
* 执行至少一次完整 trial；
* 生成下一轮 job；
* 执行下一轮 job 或至少执行到其真实训练入口和首个有效训练阶段；
* 验证 checkpoint 继承、trial lineage 和版本校验。

不得使用 mock 替代真实模型前向、反向和 optimizer step。

## Level C：当前环境可承受的最大测试

在不造成系统失控的前提下：

* 使用当前可用的最大合理数据规模；
* 使用显存安全的 batch size；
* 启用现有 small-vram、gradient accumulation、checkpointing 或分块机制；
* 运行足够长以暴露内存泄漏、状态串扰、缓存增长和周期性保存问题；
* 监控 GPU 显存、主存、磁盘增长、NaN、loss 和吞吐；
* 验证至少一个 checkpoint 可恢复；
* 验证中断后继续运行；
* 完成一个固定评估集比较。

不要因为显存较小而退回 1-step smoke。应降低 batch size、使用梯度累积、减少并行度或缩小单次窗口，同时保持足够多的 step 和样本覆盖。

如果完整数据集在当前环境确实无法运行：

1. 使用分层抽样形成最大可运行数据集；
2. 保证覆盖全部关键类别；
3. 记录实际样本、帧数、step、batch、显存和运行时间；
4. 明确说明未运行全量数据的客观原因；
5. 不得把分层抽样描述成全量测试。

# 十、错误处理原则

测试或训练失败时：

1. 定位根因；
2. 修复代码；
3. 重跑失败测试；
4. 重跑受影响链路；
5. 再继续后续大规模测试。

不得：

* 使用 `pytest.skip` 隐藏本次应完成的问题；
* 删除失败断言；
* 放宽断言到失去意义；
* 捕获所有异常后返回成功；
* 将错误改成 warning 后继续标记完成；
* 因一次失败而停止整个任务；
* 只在最终报告中列出未修复失败。

对预期的硬件 OOM：

* 自动降低 batch size 或并行度；
* 保持训练样本与 step 覆盖；
* 记录调整过程；
* 不得静默忽略。

# 十一、完成判定

只有满足以下全部条件，才可以宣布完成：

* 所有 P0 有代码、接线和测试；
* 所有 P1 有代码、接线和测试；
* 所有 P2 有代码、接线和测试；
* 所有过期文档已同步；
* 所有普通业务函数不依赖 Typer；
* 现有 CLI 外部行为保持兼容；
* 全量快速测试通过；
* 完整集成链路通过；
* artifact 可独立反加载推理；
* 版本不兼容能够被阻止；
* 自动闭环可以生成并消费下一轮 job；
* 至少完成 Level A 和 Level B；
* Level C 已完成，或有明确、可复现的客观资源限制；
* 没有新增 TODO、占位实现或被跳过的核心测试。

# 十二、最终报告格式

最终报告必须包含：

1. 检测到的运行环境；
2. 实际 Python、PyTorch、CUDA 和 GPU 信息；
3. 修改文件列表；
4. 每个 P0/P1/P2 对应的实现位置；
5. 新增或修改的配置字段；
6. schema/version 变化；
7. migration 路径；
8. 自动闭环执行流程；
9. CLI 与普通业务函数的对应关系；
10. 新增测试列表；
11. 实际执行的全部命令；
12. 单元测试结果；
13. 全量测试结果；
14. 集成测试结果；
15. Level A、B、C 的实际数据规模；
16. 实际帧数、样本数、step、batch size 和训练时长；
17. CPU、内存、GPU 显存和磁盘峰值；
18. 生成的 checkpoint、score、gallery、index、job 和 artifact 路径；
19. artifact 反加载推理结果；
20. 版本兼容校验结果；
21. 仍存在的失败或资源限制；
22. 确认没有改变项目目录结构和训练任务语义。

不要只输出“已完成”或简短摘要。报告中的每项结论必须能对应到具体文件、测试结果或生成产物。

现在直接检查仓库并开始修改。不要先只给我方案，不要等待确认，不要在完成 P0 后停止，也不要以低预算 smoke test 作为最终结果。
